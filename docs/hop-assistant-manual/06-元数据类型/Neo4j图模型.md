# Neo4j 图模型（Neo4j Graph Model）

## 核心功能

图模型通过允许您定义节点及其属性，以及连接这些节点的关系来描述（部分）图。

然后，您可以使用这样的图模型将输入字段映射到"Neo4j 图输出（Neo4j Graph Output）"转换中的属性。

## 说明

图模型是 Neo4j 集成中的核心元数据类型，用于在关系型/表格数据与图数据库之间建立映射关系。通过定义节点、属性和关系，您可以清晰地表达数据应如何被组织和存储在 Neo4j 图数据库中。

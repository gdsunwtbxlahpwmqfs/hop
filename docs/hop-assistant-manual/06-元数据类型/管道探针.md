# 管道探针（Pipeline Probe）

## 核心功能

管道探针元数据类型允许将一个管道的输出数据行流式传输到另一个管道。

接收管道随后可以处理这些数据，例如用于数据质量检查、数据画像、数据血缘等。

## 工作原理

管道探针通过指定一个接收管道（用于捕获日志的管道）来工作。该接收管道是"一个普通的管道"，它以"管道数据探针（Pipeline Data Probe）"转换作为输入转换。

接收管道随后可以利用 Qi 数据治理平台 管道提供的全部功能继续处理探针数据。

## 相关插件

- 管道探针（Pipeline Probe）

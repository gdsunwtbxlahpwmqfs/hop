# 管道单元测试（Pipeline Unit Test）

## 核心功能

描述针对管道的测试。在测试期间，会使用替代数据集作为给定转换的输入，并将输出与黄金数据集（golden data set）进行比对。

当管道执行产生的结果集与预期结果（黄金数据集）匹配时，测试通过；如果生成的结果与预期不符，则测试失败。

## 特性

- 管道中的特定转换可以被绕过或移除，以提供额外的测试灵活性。
- 单元测试可以批量执行。

## 相关插件

无特定限制，适用于所有插件。

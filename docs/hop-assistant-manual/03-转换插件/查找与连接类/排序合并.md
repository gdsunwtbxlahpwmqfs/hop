# 排序合并

排序合并（Sorted Merge）转换合并来自多个输入转换的行，前提是这些行本身已按给定键字段排序。

## 主要选项

| 选项 | 说明 |
|------|------|
| 转换名称 | 转换的名称 |
| 键字段 | 用于合并排序的键字段 |

## 注意事项

- 该转换仅支持 Hop Engine，不支持 Spark、Flink、Dataflow。
- 输入流必须已按键字段排序。
- 适用于合并多个已排序的数据流为一个有序流。

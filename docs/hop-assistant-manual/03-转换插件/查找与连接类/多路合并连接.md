# 多路合并连接

多路合并连接（Multiway Merge Join）转换连接来自多个流的输入数据。

## 主要选项

| 选项 | 说明 |
|------|------|
| 转换名称 | 转换的名称 |
| 输入流 | 要连接的多个输入流 |
| 键字段 | 各流的连接键字段 |

## 注意事项

- 该转换支持多个流的合并连接，是合并连接的多路扩展。
- 输入流必须按键字段排序。
- 适用于需要同时连接多个数据源的场景。

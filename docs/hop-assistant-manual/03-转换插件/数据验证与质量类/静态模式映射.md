# 静态模式映射（Schema Mapping）

Schema Mapping 转换将输入流中的字段映射到指定的模式定义（Schema Definition）之上。该转换的结果是一个符合所映射模式定义的输出流。

该转换用于确保数据流的结构符合预定义的规范模式，适用于数据标准化、接口契约对齐等场景，保证输出数据的字段与目标模式定义一致。

## 主要选项

| 选项 | 说明 |
|------|------|
| 模式定义 | 指定目标 Schema Definition 元数据项 |
| 字段映射 | 配置输入流字段到模式定义字段的映射关系 |

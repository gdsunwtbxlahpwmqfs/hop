# Neo4j 拆分图（Neo4j Split Graph）

Neo4j Split Graph 转换用于拆分图数据类型中的节点和关系。

该转换接收一个图类型的数据流，将其中的节点（nodes）和关系（relationships）分离成不同的输出，便于后续对节点和关系分别进行处理或分析。

## 主要选项

| 选项 | 说明 |
|------|------|
| 输入字段 | 指定包含图数据的输入字段 |
| 节点输出 | 配置节点数据的输出目标 |
| 关系输出 | 配置关系数据的输出目标 |

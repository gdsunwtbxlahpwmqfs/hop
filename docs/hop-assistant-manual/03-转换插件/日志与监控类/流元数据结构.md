# 流元数据结构（Metadata structure of stream）

Metadata Structure Of Stream 转换将该转换输入字段的元数据结构作为输出。

在产生输出之前，该转换会读取并丢弃（即"吃掉"）所有的输入行。它用于获取数据流中各字段的元数据信息（如字段名称、数据类型、长度、精度等），便于对数据结构进行文档化或动态处理。

## 主要选项

| 选项 | 说明 |
|------|------|
| 输出字段 | 配置输出元数据结构所包含的字段信息（名称、类型、长度等） |

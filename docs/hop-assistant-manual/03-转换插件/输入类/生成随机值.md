# 生成随机值

生成随机值（Generate Random Value）转换用于生成随机数、整数、字符串、UUID 等。

## 主要选项

| 选项 | 说明 |
|------|------|
| 转换名称 | 转换的名称 |
| 字段名 | 生成的随机值字段名称 |
| 类型 | 随机值的类型（如数字、整数、字符串、UUID 等） |

## 注意事项

- 该转换支持 Hop Engine、Spark、Flink、Dataflow 等多种引擎。
- 可生成多种类型的随机值，适用于测试数据生成或需要唯一标识符的场景。

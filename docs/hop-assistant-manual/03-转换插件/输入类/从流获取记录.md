# 从流获取记录

从流获取记录（Get Records From Stream）转换返回先前由工作流中另一个管道生成的记录。这些记录通过"复制行到结果（Copy rows to result）"转换或工作流执行器（Workflow Executor）转换传递到此转换。

你可以在转换中输入期望从工作流中先前管道获取的字段元数据。

## 注意事项

- **已弃用**：请改用"从结果获取行（RowsFromResult）"。
- 该转换仅支持 Hop Engine。

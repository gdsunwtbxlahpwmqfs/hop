# 在内存中加载文件内容

在内存中加载文件内容（Load File Content In Memory）转换将文件内容加载到内存中。

## 主要选项

| 选项 | 说明 |
|------|------|
| 转换名称 | 转换的名称 |
| 文件名 | 要加载到内存的文件 |
| 包括文件名字段 | 是否在输出中包含文件名字段 |
| 字段 | 输出字段定义 |

## 注意事项

- 该转换将整个文件内容加载到内存中，适用于读取较小的文本文件或二进制文件。
- 对于大文件，请注意内存消耗。

# 从文件反序列化

从文件反序列化（De-serialize From File）转换从包含行和元数据的二进制 Hop 文件中读取数据行。

## 主要选项

| 选项 | 说明 |
|------|------|
| 转换名称 | 转换的名称 |
| 文件名 | 要读取的二进制 Hop 文件 |
| 字段 | 输出字段定义 |

## 注意事项

- 该转换读取由"序列化到文件（Serialize to file）"转换写入的二进制格式文件。
- 二进制格式保留了完整的行元数据，读取效率高。

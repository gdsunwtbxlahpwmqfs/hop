# 元数据输入

元数据输入（Metadata Input）转换使你能够访问当前项目中的 Hop 元数据项。

除非过滤了一种或多种类型，否则它输出所有类型的所有元数据对象。

## 主要选项

| 选项 | 说明 |
|------|------|
| 转换名称 | 转换的名称 |
| 元数据类型 | 要过滤的元数据类型（可选） |

## 注意事项

- 该转换输出当前项目中所有元数据对象，可通过元数据类型进行过滤。
- 适用于需要读取和检查 Hop 元数据（如数据库连接、管道等）的场景。

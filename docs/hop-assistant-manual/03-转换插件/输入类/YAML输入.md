# YAML 输入

YAML 输入转换用于从 YAML 结构、文件或传入字段中读取数据。

## 主要选项

### 文件选项卡（File Tab）

| 选项 | 说明 |
|------|------|
| 转换名称 | 转换的名称 |
| 源是否定义在字段中 | 源是否来自先前转换的字段 |
| 源是否为文件名 | 源是否为文件名（否则为目录） |

## 注意事项

- 该转换可从 YAML 文件、YAML 结构或先前转换传入的字段中读取数据。
- 适用于处理 YAML 配置文件或数据交换格式。

# 获取子文件夹名

获取子文件夹名（Get SubFolder names）转换从目录中获取子文件夹名。

该转换递归遍历一个或多个目录，并为找到的每个子文件夹输出一行。这包括任意深度的所有嵌套子文件夹，而不仅仅是直接子级。

输入目录可以在转换的"文件夹"选项卡中静态配置，也可以从先前转换的字段中动态提供。每个子文件夹生成一个输出行，包含描述该文件夹的元数据字段。

## 注意事项

- 该转换递归遍历目录，包括所有嵌套子文件夹。
- 输入目录可静态配置或动态提供。

# 设置字段值（Set field Value）

设置字段值转换用于将一个字段的值替换为另一个字段的值。

## 主要选项

| 选项 | 说明 |
|------|------|
| 字段名（Field name） | 需要被替换值的目标字段名称 |
| 源字段名（Replace by field value） | 提供新值的源字段名称 |

## 注意事项

- 支持 Hop Engine 引擎；Spark、Flink、Dataflow 引擎支持情况视具体实现而定。
- 适用于字段值复制或同步的场景，例如将计算字段的值覆盖到原始字段。

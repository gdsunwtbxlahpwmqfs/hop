# 设置字段值为常量（Set field value to a constant）

设置字段值为常量转换用于将字段的值替换为您定义的硬编码常量值。

## 主要选项

| 选项 | 说明 |
|------|------|
| 字段名（Field name） | 需要设置常量值的字段名称 |
| 值（Value） | 要替换的硬编码常量值 |

## 注意事项

- 支持 Hop Engine 引擎；Spark、Flink、Dataflow 引擎支持情况视具体实现而定。
- 适用于为字段统一赋固定值的场景，例如设置状态标记或默认值。

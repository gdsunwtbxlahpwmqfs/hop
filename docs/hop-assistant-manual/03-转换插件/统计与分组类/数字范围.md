# 数字范围

数字范围（Number Range）转换将数值分组到多个预定义的范围中。

## 主要选项

| 选项 | 说明 |
|------|------|
| 转换名称 | 转换的名称 |
| 源字段 | 要分组的数值字段名 |
| 目标字段 | 输出中包含范围值的字段名 |
| 范围 | 预定义的范围列表（下限、上限、范围值） |

## 注意事项

- 该转换支持 Hop Engine、Spark、Flink、Dataflow 等多种引擎。
- 将数值映射到预定义的范围分类，例如将年龄分为"青年"、"中年"、"老年"。
- 适用于数据分箱和分类分析。

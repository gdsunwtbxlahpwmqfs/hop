# 行扁平化

行扁平化（Row Flattener）转换允许你按顺序扁平化数据。

## 主要选项

| 选项 | 说明 |
|------|------|
| 转换名称 | 转换的名称 |
| 分组字段 | 用于分组的键字段 |
| 扁平化字段 | 要扁平化的目标字段 |
| 最大目标字段数 | 扁平化后生成的最大字段数 |

## 注意事项

- 该转换将多行数据扁平化为单行中的多个列。
- 适用于将重复行数据转换为宽格式。
- 需要指定分组字段来确定哪些行应该被扁平化在一起。

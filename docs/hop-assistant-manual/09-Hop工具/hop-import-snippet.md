Hop Import 是一个命令行工具，用于将 PDI/Kettle job 和 transformation 导入 Qi Hop。
Hop Import 不仅仅是将 job 转换为 workflow、将 transformation 转换为 pipeline 的代码转换：数据库连接会被转换为 Qi Hop rdbms 连接，变量会被解析和导入，所有内容都会被打包成一个完整的 Qi Hop 项目。

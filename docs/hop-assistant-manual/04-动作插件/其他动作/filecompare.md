# 文件比较

## 描述

`File Compare` action 比较两个文件的内容。如果文件相同则返回 true，不同则返回 false。

当文件内容相同时，将执行成功的输出跳转；否则执行失败的跳转。

## 选项

| 选项 | 描述 |
|---|---|
| Action 名称 | workflow action 的名称。 |
| 文件名 1 | 要比较的第一个文件的名称和路径。 |
| 文件名 2 | 要比较的第二个文件的名称和路径。 |
| 将文件名添加到结果 | 将两个文件的文件名添加到结果中。 |

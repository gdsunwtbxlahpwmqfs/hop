# 写入日志（Write to Log）

`Write to log` 动作将特定字符串写入 Hop 日志系统。

此动作类似于写入日志（Write To Log）转换。

典型用例是将变量值和额外的自定义日志信息记录到 Hop 日志系统。

> 提示：将（未加密的）密码或其他敏感信息的变量写入 Hop 日志系统时要小心！

参数和变量必须在日志消息部分中指定。

必须设置日志级别。如果希望打印参数和变量，请将日志详细级别设置为 Basic。

在日志消息部分打印参数或变量的示例：``COUNTER: ${COUNTER}``

# XSD验证器（XSD validator）

`XSD validator` 根据内部（在 XML 文件中定义）或外部（在单独的 xsd 文件中定义）XML 架构验证 XML 文件。

XSD 是 [XML Schema](http://en.wikipedia.org/wiki/XML_Schema_(W3C)) 定义的缩写。

因此，这里有两个实体在工作：

* 要验证布局的 XML
* 描述 XML 文件外观的 XSD（架构）文件

另请参阅 XSD 验证器管道转换。

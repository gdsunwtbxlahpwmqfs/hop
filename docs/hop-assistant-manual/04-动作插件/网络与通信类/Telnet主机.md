# Telnet主机（Telnet a host）

`Telnet a host` 动作通过 [Telnet](https://en.wikipedia.org/wiki/Telnet) 协议测试特定端口上的连接。

## 主要选项

| 选项 | 说明 |
|------|------|
| 动作名称（Action name） | 工作流动作的名称。 |
| 主机名/IP（Host name/IP） | 主机。 |
| 端口（Port） | 端口（默认：23）。 |
| 超时（毫秒）（TimeOut (ms)） | 超时时间（毫秒）（默认 3000）。 |

# 发送邮件（Mail）

Mail 动作发送文本或 HTML 电子邮件，可选择附带文件附件。

此动作可用于工作流运行结束时，宣布成功运行或发送有关工作流失败的通知。

例如，在成功加载结束后，向分发列表发送电子邮件宣布加载成功并包含日志文件是很常见的做法。如果出现错误，可以发送电子邮件向分发列表中的个人发出警报。

Mail 动作需要 SMTP 服务器。您可以使用身份验证和安全性作为连接的一部分，但您必须拥有 SMTP 凭据。

您可以将文件附加到电子邮件中，例如错误日志和常规日志。此外，为方便起见，日志可以压缩为单个归档文件。

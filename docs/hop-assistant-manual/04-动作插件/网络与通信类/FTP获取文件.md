# FTP获取文件（Get a file with FTP）

## 功能概述


`Get a file with FTP` 动作从 FTP 服务器检索一个或多个文件。
此工作流动作不会"爬取"系统。例如，它不会访问远程目录并进入其他目录来查找匹配通配符的文件。此工作流仅从一个目录检索文件。

## 主要选项

### 常规选项卡

| 选项 | 说明 |
|------|------|
| 动作名称（Action name） | 工作流动作的名称。 |

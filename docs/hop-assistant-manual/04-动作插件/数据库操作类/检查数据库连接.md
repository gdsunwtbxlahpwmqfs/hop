# 检查数据库连接（Check Db connections）

`Check DB Connections` 工作流动作验证与一个或多个数据库的连接性。

## 主要选项

| 选项 | 说明 |
|------|------|
| 动作名称（Action name） | 工作流动作的名称。 |
| 连接（Connection） | 连接列表。 |

`Get connections` 按钮可检索当前项目中所有可用的关系数据库连接。

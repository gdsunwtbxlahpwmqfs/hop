# 表是否存在（Table Exists）

`Table exists` 动作验证数据库上是否存在指定的表。

您必须提供连接和表名。

Hop 根据表是否存在返回 True 或 False 值。

假设您有一个外部系统创建汇总表或昨天的数据提取。该外部系统可能尚未执行操作，因此您设置一个轮询机制来等待暂存数据到达数据库。在数据可用之前处理工作流没有意义，因此您可以使用此工作流动作作为信号量来轮询数据库以确定表是否存在。


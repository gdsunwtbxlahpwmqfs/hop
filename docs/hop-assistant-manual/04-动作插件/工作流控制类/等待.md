# 等待（Wait For）

`Wait for` 动作在工作流动作之间创建延迟。该动作允许设置等待的秒数、分钟数或小时数，然后再继续执行工作流中的下一个动作。

## 主要选项

| 选项 | 说明 |
|------|------|
| 动作名称（action name） | 工作流动作的名称。 |
| 等待时间（Wait for） | 要等待的延迟时间。 |
| 时间单位（Unit time） | 指定时间单位（秒、分钟和小时）。 |

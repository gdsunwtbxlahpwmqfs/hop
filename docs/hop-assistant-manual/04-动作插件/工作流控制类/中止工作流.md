# 中止工作流（Abort workflow）

`Abort workflow` 动作会立即停止（中止）正在运行的工作流。

如果您想显式终止工作流并将其标记为失败状态（可选附带错误消息），请使用中止动作。

## 主要选项

| 选项 | 说明 |
|------|------|
| 动作名称（Action name） | 工作流动作的名称。该名称在单个工作流中必须唯一。一个工作流动作可以多次放置在画布上，但它仍然是同一个工作流动作。 |
| 消息（Message） | 中止时添加到日志中的消息。 |

# 运行管道单元测试（Run pipeline unit tests）

`Run pipeline unit tests` 动作运行一系列选定的管道单元测试。

如果所有测试都无错误运行，则该动作成功。

错误将被记录到日志中。

## 主要选项

| 选项 | 说明 |
|------|------|
| 动作名称（Action Name） | 动作的名称。 |
| 测试名称（Test names） | 要执行的单元测试名称。 |

使用 `Get test names` 按钮可获取当前项目中可用的单元测试列表。

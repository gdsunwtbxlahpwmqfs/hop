# 设置变量（Set variables）

`Set variables` 动作在各种范围内设置变量。

## 主要选项

| 选项 | 说明 |
|------|------|
| 工作流动作名称（Workflow action name） | 工作流动作的名称。 |
| 属性文件名（Name of properties file） | 属性文件的名称。 |
| 变量范围（Variable scope） | 在 Java 虚拟机中有效；在当前工作流中有效；在父工作流中有效。 |

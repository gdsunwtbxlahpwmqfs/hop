# 创建文件夹（Create a folder）

`Create a folder` 动作在指定位置创建文件夹。

## 主要选项

| 选项 | 说明 |
|------|------|
| 动作名称（Action name） | 工作流动作的名称。 |
| 文件夹名称（Folder name） | 要创建的文件夹名称（可以是完整路径）。 |
| 文件夹存在时失败（Fail if folder exists） | 如果启用此选项，当指定文件夹已存在时，此工作流动作将失败。 |

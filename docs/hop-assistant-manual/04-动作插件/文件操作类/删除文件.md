# 删除文件（Delete File）

`Delete File` 动作删除一个文件（无论是否为空）。

## 主要选项

| 选项 | 说明 |
|------|------|
| 动作名称（Action name） | 工作流动作的名称。 |
| 文件名（File name） | 要删除的文件名和路径。 |
| 文件不存在时失败（Fail if file doesn't exist） | 当要删除的文件不再存在且此选项开启时，工作流动作将走失败输出跳。默认为关闭。 |

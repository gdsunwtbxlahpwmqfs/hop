# 压缩文件（Zip File）

`Zip file` 动作使用您在对话框中指定的选项创建标准 ZIP 归档文件。

## 主要选项

### 常规选项卡

| 选项 | 说明 |
|------|------|
| 动作名称（Action name） | 工作流动作的名称。 |
| 从上一个获取参数（Get arguments from previous） | 此复选框从先前工作流动作的结果行中获取文件名规范。从行中获取的内容：第一个字段必须包含文件名或文件夹；第二个字段应包含包含通配符。 |

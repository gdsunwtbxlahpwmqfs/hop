# 文件存在（File Exists）

`File exists` 动作验证指定文件是否存在，以及是否可以从运行 Apache Hop 的机器访问。

您必须提供文件名。

Hop 根据文件是否存在返回 True 或 False 值。

文件存在动作可以成为与其他系统集成的简单集成点。

例如，假设您有一个三部分的数据仓库加载流程。第一部分在 PERL 中运行。您有批处理脚本从远程位置访问数据、执行第一级行处理，并将数据输出到指定目录。您不希望在此完成之前启动工作流，因此将工作流放在调度器上。

# 创建文件

## 描述

`Create File` action 创建一个空文件。这在 workflow 中创建触发器文件时非常有用。

## 选项

| 选项 | 描述 |
|---|---|
| Action 名称 | workflow action 的名称。 |
| 文件名 | 要创建空文件的名称和路径。 |
| 文件存在时失败 | 当要创建的文件已存在（无论是否为空）且此选项已开启时，workflow action 将沿着失败的输出跳转执行。 |
| 将文件名添加到结果 | 将此 action 中创建的文件名添加到结果中。 |
